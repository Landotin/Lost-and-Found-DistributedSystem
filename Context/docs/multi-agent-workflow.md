# Parallel multi-agent workflow manual

This document defines the physical workspace routing and internal agent orchestration protocol used to parallelize features safely using Git Worktrees and Claude Code Native Subagents.

---

## Phase 1: Context Preparation & Scoping (Main Workspace)

1. Open your main workspace root folder and launch Claude Code:
   ```bash
   claude